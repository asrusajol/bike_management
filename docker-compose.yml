services:

  # ── Database ─────────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER:     ${POSTGRES_USER:-bikeuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-bikepass}
      POSTGRES_DB:       ${POSTGRES_DB:-bikedb}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bikeuser} -d ${POSTGRES_DB:-bikedb}"]
      interval: 10s
      timeout: 5s
      retries: 5
    # Bind to localhost only — external access not needed in production
    ports:
      - "127.0.0.1:5434:5432"

  # ── Cache / task queue ────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    ports:
      - "127.0.0.1:6379:6379"

  # ── FastAPI backend ───────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: ./backend/.env
    environment:
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-2}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "127.0.0.1:8000:8000"
    # Mount uploads dir so receipt/photo files survive container restarts
    volumes:
      - uploads_data:/app/uploads

  # ── React frontend (nginx static) ─────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Empty → axios uses relative /api/v1 URLs; nginx reverse proxy handles routing
        VITE_API_URL: ""
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
  uploads_data:
